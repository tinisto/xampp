<?xml version="1.0"?>
<ruleset name="11klassniki">
    <description>PHP_CodeSniffer rules for 11klassniki project</description>

    <!-- What to scan -->
    <file>.</file>
    
    <!-- Exclude paths -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/cache/*</exclude-pattern>
    <exclude-pattern>*/logs/*</exclude-pattern>
    <exclude-pattern>*/uploads/*</exclude-pattern>
    <exclude-pattern>*/vendor_js/*</exclude-pattern>
    <exclude-pattern>*/bower_components/*</exclude-pattern>
    <exclude-pattern>*/test_files/*</exclude-pattern>
    <exclude-pattern>deploy-*.php</exclude-pattern>
    <exclude-pattern>fix-*.php</exclude-pattern>

    <!-- Rules to use -->
    <rule ref="PSR12">
        <!-- Allow side effects in files (common in PHP web apps) -->
        <exclude name="PSR1.Files.SideEffects"/>
        <!-- Allow opening brace on same line for methods -->
        <exclude name="PSR12.Functions.OpeningFunctionBraceKernighanRitchie"/>
    </rule>

    <!-- Set encoding -->
    <arg name="encoding" value="utf-8"/>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Show colors in output -->
    <arg name="colors"/>

    <!-- Show sniff codes -->
    <arg value="s"/>

    <!-- Set memory limit -->
    <ini name="memory_limit" value="256M"/>
</ruleset>