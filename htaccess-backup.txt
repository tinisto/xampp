# Temporary simple .htaccess to fix redirect loop
RewriteEngine On

# Allow access to fix-redirect.php
RewriteRule ^fix-redirect\.php$ - [L]

# Basic routing
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]